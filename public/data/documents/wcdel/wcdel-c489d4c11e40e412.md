# Chapter01 Item Icon Generation Pipeline

## Goal

为当前第一章已经接入运行时的全部普通道具补齐图标生成准备流程，并保持与现有 `GameDefinition.Icon` 资源链路兼容。

## Scope

- 盘点当前项目内已经存在的 Chapter01 道具定义：
  - 6 件装备
  - 4 件普通道具
- 为 4 个尚未具备图标的普通道具准备批量 ImageGen prompt：
  - 小型生命药水
  - 蜂蜜面包
  - 蜂蜡块
  - 荧光草
- 新增 `Tools/ItemIcons/compose_item_icons.py`，用于把透明主体图合成为项目现有统一风格的 `256x256` 道具图标。
- 新增 `output/imagegen/chapter01_item_icons/` 任务包，保存 prompt 与目标落地清单。

## Current Behavior

- 当前图标接入仍统一走 `Assets/Game/Runtime/Core/Definitions/GameDefinition.cs` 的 `_icon` 字段。
- 第一章 6 件装备已经有图标资源与绑定。
- 第一章 4 个普通道具当前还没有正式图标资源：
  - `Item_Chapter01_SmallPotion`
  - `Item_Chapter01_HoneyBread`
  - `Item_Chapter01_BeeWax`
  - `Item_Chapter01_GlowHerb`
- 本轮已经准备好 CLI fallback 所需的批量 prompt 与后处理脚本，但真实生成阶段被 OpenAI 账户额度返回 `billing_hard_limit_reached` 阻塞，尚未产出最终 PNG。

## Asset Paths

- Prompt 包：
  - `output/imagegen/chapter01_item_icons/prompts.jsonl`
- 生成目标清单：
  - `output/imagegen/chapter01_item_icons/item_icon_jobs.json`
- 预期主体图输出目录：
  - `output/imagegen/chapter01_item_icons/generated_subjects`
- 预期正式图标目录：
  - `Assets/Game/Art/Icons/Items`

## Verification

- `python -m py_compile Tools/ItemIcons/compose_item_icons.py`
- 实际调用：
  - `python C:\Users\Admin\.codex\skills\.system\imagegen\scripts\image_gen.py generate-batch --input output/imagegen/chapter01_item_icons/prompts.jsonl --out-dir output/imagegen/chapter01_item_icons/generated_subjects --model gpt-image-1.5 --quality high --output-format png --concurrency 2 --max-attempts 2 --force`
- 结果：
  - API 请求成功发出
  - 4 个任务全部返回 `billing_hard_limit_reached`

## Next Suggestions

1. 恢复 OpenAI 图片额度后，直接重跑同一条 `generate-batch` 命令。
2. 生成出透明主体图后，执行：
   - `python Tools/ItemIcons/compose_item_icons.py --manifest output/imagegen/chapter01_item_icons/item_icon_jobs.json --template-meta Assets/Game/Art/Icons/Equipment/UI_Icon_Equipment_VillageSword.png.meta`
3. 后续补一轮编辑器侧批量绑定，把 4 个普通道具 icon 同步写回对应 ScriptableObject。
